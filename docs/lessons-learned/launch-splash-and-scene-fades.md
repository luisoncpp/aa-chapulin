# Launch splash and scene fades

The start splash already provides the launch fade. A direct case launch should stage its first plate immediately beneath that overlay; adding a second black-cover transition makes the background briefly visible before it is covered and revealed again. Keep `fadeThroughBlack` for debug launches and genuine mode/location changes where there is no splash to hide the cut.
