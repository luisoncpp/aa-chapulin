# Move Menu Labels Use Scene Names

The investigation move menu renders `InvestigationScene.name` before `title`. If two locations share a building, their optional `name` values must include the distinguishing room or level; distinct scene titles alone are not enough.
